[package]
name = "campaign_factory"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = "22.0.0"
campaign_escrow = { path = "../campaign_escrow" }

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
